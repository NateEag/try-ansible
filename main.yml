---
# This Ansible playbook is just a test of setting up various corners of a web
# dev box automatically.

- name: Do everything
  # TODO Doesn't *really* apply to all hosts - just a dev setup.
  hosts: all
  tasks:
    - name: Set hostname
      sudo: yes
      hostname: name=dev.nateeag
